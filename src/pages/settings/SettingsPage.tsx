import { PlaceholderPage } from '@shared/ui/PlaceholderPage';

export function SettingsPage() {
  return (
    <PlaceholderPage
      title="设置页"
      route="/settings"
      description="导出 / 导入 / 清空 / 关于 — Sprint 4 实现"
    />
  );
}
